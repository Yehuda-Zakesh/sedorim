[package]
name = "seder-plus-shared"
version = "1.0.0"
description = "The shared data file, the problem log and the background reminder rules"
edition = "2021"
rust-version = "1.77.2"
publish = false

[lib]
name = "seder_plus_shared"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
